<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" xmlns:local="clr-namespace:YoutubeDownloader">

    <!--  Progressbar Style  -->
    <Style x:Key="ProgressBarStyle" TargetType="ProgressBar">
        <Setter Property="Height" Value="30" />
        <Setter Property="Margin" Value="10" />
        <Setter Property="Foreground" Value="{DynamicResource CyanColor}" />
        <Setter Property="Maximum" Value="100" />
        <Setter Property="Minimum" Value="0" />
    </Style>

    <Style x:Key="CaptionProgressBar" TargetType="local:CaptionProgressBar">
        <Setter Property="Visibility" Value="Hidden" />
        <Style.Triggers>
            <DataTrigger Binding="{Binding State}" Value="{x:Static local:Mp3ModelState.Downloading}">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
            <DataTrigger Binding="{Binding State}" Value="{x:Static local:Mp3ModelState.Converting}">
                <Setter Property="Visibility" Value="Visible" />
            </DataTrigger>
        </Style.Triggers>
    </Style>


</ResourceDictionary>